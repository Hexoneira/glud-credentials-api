package org.glud.credentials.security.middleware;

/**
 * Middleware para la autorización basada en roles.
 * Esta clase está prevista para una futura iteración.
 * La implementación actual se centra únicamente en la autenticación (Incidencias n.º 2 y n.º 3 - iteración 1).
 *
 * Tareas pendientes: Implementar el control de acceso basado en roles (RBAC) en un sprint posterior.
 */
public class RequiredRole {
    // Nota: Se implementará en la próxima iteración, cuando se defina la gestión de roles y permisos.
}


