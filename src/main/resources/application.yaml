spring:
  application:
    name: credentials
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/credentials}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
